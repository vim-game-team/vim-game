line: chars between the first char, and either a newline-char or a Static tile (e.g. empty, wall)
text field: connected char tiles 
VP: (viewport) currently visible area
GC: GameConfig
cartesian/cart: "Kartesische koordinaten" where the middle (e.g. of the map) is (0, 0)