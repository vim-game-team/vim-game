export enum CmdType {
    MOTION = "motion",
    OPERATOR = "operator",
    TEXTOBJ = "textobj",
    NONE = "none",
}
export enum TileType {
    EMPTY = "empty",
    GROUND = "ground",
    WALL = "wall"
}

export enum Action{
    MOVE,
    DELETE,
}